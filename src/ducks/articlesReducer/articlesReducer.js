//set inital state
var initalState = {
    articles: []
}

function reducer(state = initalState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
export default reducer;