//set inital state
var initalState = {
    newsletters: []
}

function reducer(state = initalState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
export default reducer;